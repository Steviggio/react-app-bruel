const API_URL = "http://localhost:5678";
export const API_ROUTES = {
  SIGN_IN: `${API_URL}/api/users/login`,
  PROJECTS: `${API_URL}/api/works`,
  CATEGORIES: `${API_URL}/api/categories`
}

export const APP_ROUTES = {
  SIGN_IN: "/login",
}